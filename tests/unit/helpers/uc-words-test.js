import { ucWords } from '../../../helpers/uc-words';
import { module, test } from 'qunit';

module('Unit | Helper | uc words');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ucWords(42);
  assert.ok(result);
});
