import { regexpReplace } from '../../../helpers/regexp-replace';
import { module, test } from 'qunit';

module('Unit | Helper | regexp replace');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = regexpReplace(42);
  assert.ok(result);
});
